# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/

$ ->
  $(document).on 'change', '#item_category', (evt) ->
    $.ajax 'update_sub_categories',
      type: 'GET'
      dataType: 'script'
      data: {
        category_id: $("#item_category_id option:selected").val()
      }
      error: (jqXHR, textStatus, errorThrown) ->
        console.log("AJAX Error: #{textStatus}")
        alert("error")
      success: (data, textStatus, jqXHR) ->
        console.log("Dynamic subcategory script is called")
  $('#item_category').autocomplete source: (request, response) ->
    jQuery.get 'autocomplete_categories', { query: request.term }, (data) ->
      $.each data, (_, item) ->
        item.label = item.name
        return
      response(data);
      return
    return
  $('#item_sub_category').autocomplete source: (request, response) ->
    jQuery.get 'autocomplete_sub_categories', { query: request.term }, (data) ->
      $.each data, (_, item) ->
        item.label = item.name
        return
      response(data);
      return
    return
  $('#item_name').autocomplete source: (request, response) ->
    jQuery.get 'autocomplete_item_names', { query: request.term }, (data) ->
      $.each data, (_, item) ->
        item.label = item.name
        return
      response(data);
      return
      return
  $('#item_store').autocomplete source: (request, response) ->
    jQuery.get 'autocomplete_stores', { query: request.term }, (data) ->
      $.each data, (_, item) ->
        item.label = item.name
        return
      response(data);
      return
      return